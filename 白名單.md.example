# 加班單簽核白名單

以下人員的**加班單簽核**（事後加班，非預定加班單）可直接簽核，不需每次徵求同意：

- 王小明（000001）
- 陳小華（000002）
- 林小美
- 張小強
- 李小芳（000003）

新增/移除白名單成員時，直接編輯此檔案即可。

---

**這是範例檔案。** 真實使用時，複製這個檔案為 `白名單.md`（同目錄下，
`.gitignore` 已排除該檔名，不會被誤 commit），並填入實際姓名/工號。
姓名比對是**完整字串比對**（`check-queues.js` 讀取「姓名」欄位做 exact
match），工號目前僅供人類參考，不參與比對邏輯。
