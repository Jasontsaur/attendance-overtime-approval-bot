# Live session cookies — never commit. Regenerate via login-once.js or
# build-storage-state-from-cookie.js (see playwright/README section in main README).
playwright/auth/

# Regenerable npm deps
playwright/node_modules/

# Regenerable user-local shared libs (apt-get download + dpkg-deb -x output).
# See playwright/localdeps/README.md to reproduce.
playwright/localdeps/debs/
playwright/localdeps/root/

# Real coworker names/employee IDs — not for a shared repo.
白名單.md

# Misc OS/editor cruft
.DS_Store
*.log
